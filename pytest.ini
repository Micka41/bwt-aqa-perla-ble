[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings = ignore::DeprecationWarning

# Aucun test ne doit dépasser quelques secondes : un dépassement signale une
# attente réelle laissée dans le code (les timeouts BLE sont raccourcis par les
# fixtures) plutôt qu'un test lent.
timeout = 30
